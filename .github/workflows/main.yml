name: RDP Setup and Software Installation

on:
  workflow_dispatch:

jobs:
  setup-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Configure Core RDP Settings
        run: |
          # Enable RDP
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" `
            -Name "fDenyTSConnections" -Value 0 -Force

          # Disable NLA
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" `
            -Name "UserAuthentication" -Value 0 -Force

          # Allow low-security RDP encryption layer
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" `
            -Name "SecurityLayer" -Value 0 -Force

          # Allow 3389 in firewall
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389

          Restart-Service -Name TermService -Force

      - name: Create RDP User with Fixed Password
        run: |
          $password = 'Zain.rty123'
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force

          # Ensure Administrator password updated
          Set-LocalUser -Name "Administrator" -Password $securePass

          # Ensure admin groups
          Add-LocalGroupMember -Group "Administrators" -Member "Administrator" -ErrorAction SilentlyContinue
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "Administrator" -ErrorAction SilentlyContinue

          echo "RDP_PASS=$password" >> $env:GITHUB_ENV

      - name: Disable RDP Session Timeout / Idle Lock / Keep Alive
        run: |
          # Disable idle timeout
          Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server" `
            -Name IdleTimeout -Value 0 -Force

          # Disable max disconnect time
          Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server" `
            -Name MaxDisconnectTime -Value 0 -Force

          # Disable max idle time
          Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server" `
            -Name MaxIdleTime -Value 0 -Force

          # Disable Winlogon inactivity lock
          Set-ItemProperty -Path "HKLM:\Software\Microsoft\Windows\CurrentVersion\Policies\System" `
            -Name "InactivityTimeoutSecs" -Value 0 -Force

          # TCP KeepAlive every 5 minutes
          New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters" `
            -Name KeepAliveTime -PropertyType DWord -Value 300000 -Force

          # RDP client keepalive
          New-ItemProperty -Path "HKLM:\Software\Microsoft\Terminal Server Client" `
            -Name "KeepAliveTimeout" -Value 1 -PropertyType DWord -Force

          # Disable sleep & hibernate
          powercfg -change -standby-timeout-ac 0
          powercfg -change -hibernate-timeout-ac 0

      - name: Remove GitHub Auto-Logout Tasks
        run: |
          schtasks /Delete /TN "GitHub Actions Idle Timeout" /F 2>$null
          schtasks /Delete /TN "GitHub Actions Automatic Logout" /F 2>$null

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"

          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          $authKey = "${{ secrets.TAILSCALE_AUTH_KEY }}"
          
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$authKey --hostname=gh-runner-$env:GITHUB_RUN_ID
          
          $tsIP = $null
          for ($i=0; $i -lt 10; $i++) {
            $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
            if ($tsIP) { break }
            Start-Sleep 5
          }
          
          if (-not $tsIP) { throw "No Tailscale IP assigned." }
          
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Verify RDP Accessibility
        run: |
          Write-Host "Testing RDP Port 3389 on $env:TAILSCALE_IP"
          $res = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389

          if (-not $res.TcpTestSucceeded) { throw "RDP not reachable." }
          Write-Host "RDP port is reachable!"

      - name: Download and Extract SOFTWARES
        run: |
          $downloadUrl  = "https://github.com/Zaiinalii/RDP/releases/download/v1/sOFTWARES.rar"
          $downloadPath = "$env:TEMP\sOFTWARES.rar"
          $extractPath  = "$env:TEMP\sOFTWARES_extracted"
          $desktopPath  = Join-Path $env:USERPROFILE "Desktop"

          Invoke-WebRequest -Uri $downloadUrl -OutFile $downloadPath -ErrorAction Stop

          # Ensure 7zip exists
          if (-not (Test-Path "C:\Program Files\7-Zip\7z.exe")) { choco install 7zip -y }
          $sevenZip = "C:\Program Files\7-Zip\7z.exe"

          # Extract
          if (Test-Path $extractPath) { Remove-Item $extractPath -Recurse -Force }
          New-Item -ItemType Directory -Path $extractPath | Out-Null

          & $sevenZip x $downloadPath "-o$extractPath" -y

          # Copy to desktop
          Get-ChildItem $extractPath | ForEach-Object {
            $d = Join-Path $desktopPath $_.Name
            Copy-Item $_.FullName $d -Recurse -Force
          }

          Remove-Item $downloadPath -Force
          Remove-Item $extractPath -Recurse -Force

      - name: Maintain RDP Connection
        run: |
          Write-Host "`n===== RDP ACCESS ====="
          Write-Host "IP Address:     $env:TAILSCALE_IP"
          Write-Host "Username:       Administrator"
          Write-Host "Password:       $env:RDP_PASS"
          Write-Host "=========================`n"

          while ($true) {
            Write-Host "[$(Get-Date)] RDP Session Active — Running..."
            Start-Sleep -Seconds 300
          }
